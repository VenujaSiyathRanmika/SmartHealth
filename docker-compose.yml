version: "3.9"

services:

  backend:
    build: ./backend
    ports:
      - "5002:5000"

  notification-service:
    build: ./notification-service
    ports:
      - "5001:5001"

  frontend:
    build: ./frontend
    ports:
      - "5173:5173"